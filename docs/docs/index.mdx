---
sidebar_position: 1
sidebar_label: 🏠 Home
title: macaron - Python Project Template
---

import React, { useState, useEffect } from 'react';
import { Card } from '@site/src/components/ui/card';
import { motion } from 'framer-motion';
import clsx from 'clsx';
import { Separator } from '@site/src/components/ui/separator';
import Link from '@docusaurus/Link';
import DocsFeatures from '@site/src/components/DocsFeatures';

export default function HomePage() {
  const subtitles = [
    'Start with the best',
    'Build with confidence',
    'Accelerate your development',
  ];

  const [currentSubtitle, setCurrentSubtitle] = useState(0);

  useEffect(() => {
    const interval = setInterval(() => {
      setCurrentSubtitle((prev) => (prev + 1) % subtitles.length);
    }, 3000);
    return () => clearInterval(interval);
  }, [])

  return (
    <div className="container mx-auto px-6 py-12">
      {/* Header Section */}
      <motion.div
        className="flex flex-col items-center"
        initial={{ opacity: 0 }}
        animate={{ opacity: 1 }}
        transition={{ duration: 1 }}
      >
        <Card className="w-full max-w-4xl p-8 bg-gradient-to-r from-blue-500 via-purple-500 to-pink-500 shadow-xl rounded-xl">
          <div className="flex flex-col md:flex-row items-center justify-between">
            <motion.div
              className="flex items-center space-x-4"
              initial={{ x: -50 }}
              animate={{ x: 0 }}
              transition={{ duration: 0.5 }}
            >
              <motion.img
                src="/img/icon.webp"
                alt="Macaron Icon"
                className="h-20 w-20"
                initial={{ rotate: -180 }}
                animate={{ rotate: 0 }}
                transition={{ duration: 1 }}
              />
              <h1 className="text-6xl font-extrabold text-white">macaron</h1>
            </motion.div>
            <motion.h2
              key={currentSubtitle}
              className="mt-6 md:mt-0 text-2xl font-semibold text-white"
              initial={{ opacity: 0 }}
              animate={{ opacity: 1 }}
              transition={{ duration: 0.5 }}
            >
              {subtitles[currentSubtitle]}
            </motion.h2>
          </div>
          <Separator className="my-6 border-t-2 border-white" />
          <p className="text-center text-xl text-white">
            <span className="macaron text-xl">macaron</span> bundles the best Python
            project resources into a{' '}
            <a
              href="https://github.com/cookiecutter/cookiecutter"
              target="_blank"
              rel="noopener noreferrer"
              className="font-semibold italic hover:text-gray-200"
            >
              <img height="1rem" width="auto" src="/img/icons/cookiecutter.png" className="h-6 w-6 inline-block" alt="Cookiecutter" />
              &nbsp;cookiecutter
            </a>{' '}
            project template.
          </p>
        </Card>
      </motion.div>

      {/* Features Section */}
      <DocsFeatures />

      <Separator className="my-12 border-t-2 border-gray-300 dark:border-gray-700" />

      {/* Call to Action Section */}
      <div className="text-center">
        <motion.p
          className="text-3xl font-semibold text-gray-900"
          initial={{ scale: 0.8 }}
          animate={{ scale: 1 }}
          transition={{ duration: 0.5 }}
        >
          Kickstart your next Python project with{' '}
          <span className="macaron text-4xl">macaron</span>!
          <img
            src="/img/icon.png"
            alt="Macaron Icon"
            className="inline-block h-10 w-10 ml-2"
          />
        </motion.p>
      </div>
    </div>
  );
}

# <span>HomePage</span>

<HomePage />